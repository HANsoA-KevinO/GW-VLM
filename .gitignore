# 虚拟环境（在新机器上重建，不传输）
.venv/
__pycache__/
*.pyc

# output 整体不入库：全部可由 01-07 确定性再生，且 dataset jsonl 含机器相关绝对路径
output/

# 不属于本项目（独立的 scoping review 论文，含版权 PDF）/ 旧重复目录
scoping_review/
research_design/

# 训练产物（未来）
training_data/
checkpoints/
*.safetensors
wandb/

# 论文 PDF（版权 + 体积大，~287MB）：只入库索引/清单/下载脚本，PDF 用 docs/papers/download.sh 复现
docs/papers/*/
docs/papers/*.pdf

# 系统文件
.DS_Store
.venv-inject/
